services:
  - type: web
    name: adaptive-cutoff-demo
    env: python
    buildCommand: pip install .
    startCommand: python -m adaptive_cutoff_demo.dashboard
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
